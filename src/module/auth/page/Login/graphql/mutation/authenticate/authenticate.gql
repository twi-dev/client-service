mutation Authenticate($credentials: AuthInput!) {
  authenticate(credentials: $credentials) {
    refreshToken {
      type
      payload
    }
    accessToken {
      type
      payload
      expires
    }
  }
}
