import {h} from "preact"

import {container, box, title} from "../style/form.sss"

const Home = () => (
  <div class={container}>
    <div class={box}>
      <div class={title}>Signin</div>
    </div>
  </div>
)

export default Home
