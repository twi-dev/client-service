#import "common/graphql/fragment/story/minimal.gql"
#import "common/graphql/fragment/common/dates.gql"

query GetStory($slug: String!) {
  story(slug: $slug) {
    ...storyMinimal

    publisher {
      login
    }

    dates {
      ...commonDates
    }
  }
}
